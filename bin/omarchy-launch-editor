#!/bin/bash

omarchy-cmd-present "$EDITOR" || EDITOR=nvim

case "$EDITOR" in
nvim | vim | nano | micro | hx | helix)
  exec setsid uwsm-app -- xdg-terminal-exec --app-id=com.omarchy.$EDITOR "$EDITOR" "$@"
  ;;
*)
  exec setsid uwsm-app -- "$EDITOR" "$@"
  ;;
esac
